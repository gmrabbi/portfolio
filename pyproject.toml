[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "portfolio"
version = "1.0.0"
description = "AI/ML Engineer Portfolio Website"
authors = [{name = "Golam Mostafa Rabby"}]
requires-python = ">=3.8"

[tool.vercel]
entrypoint = "app.py"
